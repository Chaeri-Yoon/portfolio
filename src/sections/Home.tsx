import styled from "styled-components";

const Container = styled.div`
    width: 100%;
    padding: 12.5em 0;

    // This will be replaced with the picture of mine or anything like that
    background-color: #f2dada;
    opacity: 0.5;
`;

export default () => {
    return (
        <Container />
    )
}